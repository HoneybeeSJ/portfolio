# 허니비 디자인 시스템 (Honeybee Design System)

> 이승재 포트폴리오 · 2026-06-11 정의. 브랜드 "사람의 성장 × AI의 속도" + 허니비 상징.
> 방향: **꿀벌 옐로우 + 블랙 / 완전 교체(퍼플·시안 은퇴) / 미니멀·프리미엄 / 다크(근적) 베이스.**
> 본 문서는 스펙 정의용. 실제 CSS 적용은 다음 단계(§7 매핑표 사용).

---

## 1. 디자인 원칙
1. **블랙이 캔버스, 옐로우는 강조.** 근적 블랙이 화면의 80%, 옐로우는 10% 안팎의 포인트로만.
2. **벌집 = 구조.** 장식이 아니라 "구조화"라는 브랜드 메시지를 나르는 모티프. 육각형을 절제되게.
3. **미니멀·프리미엄.** 줄무늬·카툰 벌 지양. 여백과 대비로 고급스럽게.
4. **가독성 우선.** 옐로우 위에는 반드시 블랙 텍스트. 옐로우 텍스트는 다크 위에서만.

## 2. 컬러 토큰
**60-30-10 비율** — 근적 블랙 ~70%, 화이트/그레이 텍스트 ~20%, 옐로우 액센트 ~10%.

### 배경 (Base)
| 토큰 | HEX | 용도 |
|---|---|---|
| `--bg` | `#0E0E10` | 메인 배경(근적 블랙) |
| `--bg2` | `#16161A` | 카드·상승 표면 |
| `--ink` | `#08080A` | 최대 대비 블록·푸터 |

### 옐로우 (Honey / Primary)
| 토큰 | HEX | 용도 |
|---|---|---|
| `--honey` | `#FFC400` | 주 액센트(CTA·핵심 수치·.accent) |
| `--honey-bright` | `#FFD43B` | 하이라이트·호버 |
| `--honey-deep` | `#C99700` | 보더·그라데이션 끝·머티드 |
| `--honey-soft` | `rgba(255,196,0,0.12)` | 틴트 배경·글로우 |

### 텍스트 & 글래스
| 토큰 | HEX | 용도 |
|---|---|---|
| `--text` | `#F2F2F3` | 본문(근백색) |
| `--text-muted` | `#A1A1AA` | 보조 텍스트 |
| `--on-honey` | `#0A0A0A` | 옐로우 위 텍스트(필수 블랙) |
| `--glass-bg` | `rgba(255,255,255,0.04)` | 글래스카드 배경 |
| `--glass-border` | `rgba(255,255,255,0.08)` | 글래스 보더 |
| `--honey-border` | `rgba(255,196,0,0.28)` | 강조 카드 보더·육각형 라인 |

### 접근성 체크 (WCAG)
- `--honey #FFC400` on `--bg #0E0E10` → 약 12:1 ✅ (액센트·큰 텍스트 안전)
- `--on-honey #0A0A0A` on `--honey #FFC400` → 약 13:1 ✅ (버튼 텍스트)
- `--text #F2F2F3` on `--bg` → 약 17:1 ✅
- `--text-muted #A1A1AA` on `--bg` → 약 7:1 ✅
- ⛔ 금지: 옐로우 위 화이트, 화이트 위 옐로우 텍스트(대비 미달)

## 3. 타이포그래피 (현행 유지)
- 디스플레이/제목: **Space Grotesk** + Noto Sans KR, 700
- 본문: **Noto Sans KR** 300–500 / Inter
- 규칙: 제목은 굵게·큰 대비, 본문 line-height 1.9 전후. 폰트는 바꾸지 않음(이미 프리미엄).
- `.accent`(강조 텍스트) 색상만 퍼플 → `--honey`.

## 4. 육각형 모티프 (Hexagon)
브랜드의 핵심 시각 장치. **"구조화"를 상징** — 절제가 생명.
- **배경 텍스처**: 육각형 타일링을 아주 낮은 불투명도(0.04–0.06)로 히어로·섹션 구분에. 거의 안 보일 정도.
- **솔리드 육각형**: 작은 요소에만 — 번호 배지(01·02), 리스트 불릿(▸ 대신 작은 ⬡), 아이콘 프레임, 로고 닷.
- **카드 액센트**: 카드 모서리/상단에 옐로우 라인 또는 육각형 코너 컷.
- ⛔ 금지: 큰 면적을 밝은 옐로우 육각형으로 채우기, 벌 캐릭터·이모지 남발(🐝는 파비콘 등 1곳 이내).

### 줄무늬(Bee Stripe) — 선택·최소
- 허용: 4px 이하 얇은 옐로우 강조 바, 구분선 1곳 정도.
- ⛔ 경고테이프처럼 보이는 대각 옐로우/블랙 줄무늬 큰 면적은 금지(프리미엄 톤 훼손).

## 5. 컴포넌트 규칙
- **버튼 Primary(CTA)**: 배경 `--honey`, 텍스트 `--on-honey`, 호버 `--honey-bright`. 굵게.
- **버튼 Ghost**: 투명, 보더 `--honey-border`, 텍스트 `--honey`, 호버 배경 `--honey-soft`.
- **글래스카드**: 글래스 유지. 강조 시 상단/좌측 라인 그라데이션 `--honey → --honey-deep`(기존 퍼플→시안 대체).
- **sec-tag**: 보더·텍스트 `--honey`.
- **핵심 수치(stat/impact)**: 숫자 `--honey`, 단위 작게.
- **태그/칩**: 기본 글래스 보더; highlight-chip 배경 `--honey-soft` + 텍스트 `--honey-bright`.
- **로고 닷** `이승재.`: 점을 작은 옐로우 육각형 또는 `--honey` 닷으로.

## 6. 다크↔라이트 / 모션
- 베이스는 다크 고정. (라이트 버전은 추후 필요 시: 배경 `#FAFAF7`, 텍스트 `#0A0A0A`, 옐로우 포인트.)
- 모션: 기존 GSAP 리빌·카운트업 유지. 호버에 옐로우 글로우(`box-shadow: 0 0 0 1px var(--honey-border)`) 절제 사용.

## 7. 기존 CSS 변수 매핑표 (적용 단계용)
`css/style.css`의 `:root`를 아래로 치환하면 일괄 리스킨됨.

| 기존 | 신규 |
|---|---|
| `--bg: #0d0d1f` | `--bg: #0E0E10` |
| `--bg2: #12112a` | `--bg2: #16161A` |
| `--purple: #8b5cf6` | `--honey: #FFC400` |
| `--purple-d: #7c3aed` | `--honey-deep: #C99700` |
| `--cyan: #06b6d4` | `--honey-bright: #FFD43B` (시안 은퇴) |
| `--emerald: #10b981` | 제거(또는 success 단일 용도만) |
| `--text: #e2e8f0` | `--text: #F2F2F3` |
| `--text-muted: #94a3b8` | `--text-muted: #A1A1AA` |
| `--glass-shadow` | 유지(검정 그림자) |

**추가 작업(적용 시 체크리스트)**
- `body` 배경 radial-gradient의 퍼플·시안 글로우 → `--honey-soft` 글로우(저불투명)로 교체, 근적 베이스.
- 모든 `.accent` 퍼플 → `--honey`.
- 카드 `::before` 그라데이션(퍼플→시안) → `--honey → --honey-deep`.
- 버튼/보더/닷 토큰 일괄 점검.
- 3D 캔버스(hero-scene.js·ca-scene.js)의 입자·라인 컬러도 옐로우 계열로 교체.
- 육각형 배경 텍스처 추가(저불투명).

## 8. 적용 범위 (다음 단계)
메인 포트폴리오 + 자기소개.html 동시 적용. style.css 변수 교체 → 컴포넌트 점검 → 3D 씬 컬러 → 육각형 텍스처 → 브라우저 렌더 확인.
